import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();
  
  // Eczanem - Turkish Pharmacy Brand Colors (Red & White)
  static const primaryColor = Color(0xFFDC143C);      // Turkish Pharmacy Red (Crimson)
  static const secondaryColor = Color(0xFFE31837);    // Bright Red
  static const accentColor = Color(0xFFFF4757);       // Light Red Accent
  
  static const backgroundColor = Color(0xFFFAFAFA);   // Off-White Background
  static const cardColor = Color(0xFFFFFFFF);         // Pure White Cards
  
  static const textColor = Color(0xFF2C3E50);         // Dark Text
  static const secondaryTextColor = Color(0xFF95A5A6); // Gray Text
  
  static const borderColor = Color(0xFFECF0F1);       // Light Border
  static const successColor = Color(0xFF27AE60);      // Success Green
  static const warningColor = Color(0xFFF39C12);      // Warning Orange
  static const errorColor = Color(0xFFE74C3C);        // Error Red
  
  // Turkish Pharmacy Specific
  static const pharmacyRed = Color(0xFFDC143C);       // Classic Red Cross Color
  static const pharmacyWhite = Color(0xFFFFFFFF);     // Pure White
}