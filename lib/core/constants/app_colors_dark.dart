import 'package:flutter/material.dart';

class AppColorsDark {
  const AppColorsDark._();
  static const primaryColor = Color(0xFF3498db); // Keep same blue
  static const secondaryColor = Color(0xFFff7c06); // Keep same orange
  static const backgroundColor = Color(0xFF121212); // Dark background
  static const surfaceColor = Color(0xFF1E1E1E); // Card/surface color
  static const textColor = Color(0xFFE0E0E0); // Light text
  static const secondaryTextColor = Color(0xFFB0B0B0); // Secondary light text
  static const borderColor = Color(0xFF333333); // Dark border
}